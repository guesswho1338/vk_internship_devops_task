# vk_internship_devops_task

Script is working only for Linux Distros with systemd

# Install and start monitoring

```
chmod +x install.sh
sudo ./install.sh
```

`sudo` is needed for installing systemd unit


